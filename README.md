# CS-460-Group-Project
Run and verify with the following commands, make sure test files are present in their test_data folder,which is at the same level as source files <br>
Note that the shell script will output successful test confirmations<br>
chmod 750 run_tests <br>
./run_tests

